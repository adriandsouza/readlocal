# Contributing

Use Node 22+. Run `npm install`, `npm run models:download`, then `npm run lint && npm run typecheck && npm test && npm run build`. Keep processing local, add no telemetry or third-party runtime requests, and include focused tests. Never commit models or private PDF fixtures. Contributions are MIT-licensed.
