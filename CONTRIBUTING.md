# Contributing

Use Node 22+, run `npm install`, then `npm run lint && npm run typecheck && npm test && npm run build`. Keep processing local, add no telemetry, and include focused tests. Contributions are MIT-licensed.
