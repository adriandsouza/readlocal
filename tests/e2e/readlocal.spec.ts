import { expect, test } from '@playwright/test'
test('opens without unexpected requests and rejects a corrupt PDF',async({page})=>{const unexpected:string[]=[];page.on('request',(request)=>{if(!request.url().startsWith('http://127.0.0.1:5173'))unexpected.push(request.url())});await page.goto('/');await expect(page.getByText('Your document stays on your device.')).toBeVisible();await page.getByLabel('Select PDF').setInputFiles({name:'readlocal-test.pdf',mimeType:'application/pdf',buffer:Buffer.from('%PDF-1.4\n%%EOF')});await expect(page.getByRole('alert')).toContainText(/corrupted|unsupported/);expect(unexpected).toEqual([])})
